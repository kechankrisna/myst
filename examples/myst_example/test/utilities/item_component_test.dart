import 'package:flutter_test/flutter_test.dart';
import 'package:myst_example/core.dart';

void main() {
  group("ItemComponent", () {
    late ItemComponent itemComponent;

    setUp(() {
      // TODO: initialize itemComponent to test the behavior
      itemComponent = ItemComponent();
    });

    test(": ==", () {
      expect(true, true);
    });
  });
}
