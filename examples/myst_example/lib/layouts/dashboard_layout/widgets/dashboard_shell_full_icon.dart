import 'package:myst_example/core.dart';

class DashboardShellFullIcon extends StatelessWidget {
  const DashboardShellFullIcon({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return IconButton(
      icon: Icon(MdiIcons.windowMaximize),
      onPressed: () {},
    );
  }
}
