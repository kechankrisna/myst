import 'package:myst_example/core.dart';

class DashboardKeyboardIcon extends StatelessWidget {
  const DashboardKeyboardIcon({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return IconButton(
      icon: Icon(MdiIcons.keyboard),
      
      onPressed: (){},
    );
  }
}