import 'package:myst_example/core.dart';

class DashboardShellSettingIcon extends StatelessWidget {
  const DashboardShellSettingIcon({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return IconButton(
      icon: Icon(MdiIcons.cog),
      
      onPressed: (){},
    );
  }
}