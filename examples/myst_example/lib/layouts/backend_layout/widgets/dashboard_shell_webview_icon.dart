import 'package:myst_example/core.dart';

class DashboardShellWebviewIcon extends StatelessWidget {
  const DashboardShellWebviewIcon({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return IconButton(
      icon: Icon(MdiIcons.web),
      onPressed: () {},
    );
  }
}
